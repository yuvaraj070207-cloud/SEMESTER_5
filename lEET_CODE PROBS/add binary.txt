#include <stdlib.h>
#include <string.h>

char* addBinary(char* a, char* b) {
    int lenA = strlen(a);
    int lenB = strlen(b);
    int i = lenA - 1;
    int j = lenB - 1;
    int carry = 0;
    
    // The maximum possible length of the sum is max(lenA, lenB) + 1
    int maxLen = lenA > lenB ? lenA : lenB;
    
    // Allocate memory: maxLen + 1 (for extra carry) + 1 (for '\0')
    char* result = (char*)malloc((maxLen + 2) * sizeof(char));
    result[maxLen + 1] = '\0';
    
    int k = maxLen;
    while (i >= 0 || j >= 0 || carry) {
        int sum = carry;
        if (i >= 0) {
            sum += a[i--] - '0';
        }
        if (j >= 0) {
            sum += b[j--] - '0';
        }
        
        result[k--] = (sum % 2) + '0';
        carry = sum / 2;
    }
    
    // If the leading space wasn't used by an extra carry bit, shift string left
    if (k >= 0) {
        memmove(result, result + k + 1, maxLen + 1 - k);
    }
    
    return result;
}